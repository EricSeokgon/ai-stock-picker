---
name: stock-picker-spec-conventions
description: SPEC conventions for the ai-stock-picker (Korean Stock & ETF Recommendation System) project
metadata:
  type: project
---

ai-stock-picker 프로젝트(한국 주식 & ETF 추천 시스템)의 SPEC 작성 규약.

**Facts:**
- SPEC ID 체계: `SPEC-STOCK-NNN` (001 = MVP/Phase 1·2, 002 = Phase 3 인증·텔레그램·포트폴리오·백테스팅, 003 = Phase 4 WebSocket 실시간 시세·관심목록·포트폴리오 AI 분석, 004 = Phase 5 알림 고도화 — 관심목록 가격 알림(텔레그램)·이메일 알림(SMTP), 005 = Phase 6 성능·UX 고도화 — Redis 캐싱(추천/현재가)·추천 필터·정렬 API·모바일 반응형, 006 = Phase 7 AI 분석 고도화·추천 근거 투명성 — Claude explanation 컬럼·추천 히스토리 API·뉴스 감성 5단계 라벨, 007 = Phase 8 탐색성·시각화·신뢰 피드백 — 종목 검색(GET /stocks/search, krx_master 기반)·종목 상세 페이지 + 30일 가격 차트(GET /stocks/{krx_code}/prices, prices.py 재사용·Redis 캐시, 신규 라우터 stocks)·추천 품질 피드백(recommendation_feedback 테이블·POST|GET /recommendations/{krx_code}/feedback, 수집·표시만/재가중은 제외)). 008 = Phase 9 섹터 분석 대시보드 — 누락된 섹터 집계 생산자 구현(AnalysisResult.sector_tags+sentiment_score→sector_trends upsert)·파이프라인 연결·섹터 순위(GET /sectors/ranking)·섹터 상세(GET /sectors/{sector}/detail, 구성종목=StockMention→Article→AnalysisResult 조인)·프론트 /sectors 비교 페이지. spec/tasks/acceptance/progress 4파일. 마이그레이션 0011=(sector,trade_date) UNIQUE 제약(upsert용). 피드백 재가중·섹터 알림은 범위 밖. 009 = Phase 10 추천 품질 개선 — 피드백 기반 가중치 + 스코어 투명성. SPEC-007에서 수집만 하고 미활용이던 recommendation_feedback(up/down)을 점수에 반영하는 첫 SPEC. 기본 점수(base_score) 계산 후 별도 조정 단계로 피드백 계수(신뢰도 가중+[-MAX_ADJ,+MAX_ADJ] 클램프) 적용→조정 점수(total_score). 4요인 산식(0.40/0.20/0.25/0.15, scoring/engine.py)은 변경 금지. 스코어 분해(요인별 가중 기여도=weight×score) 상세 응답·프론트 ScoreBreakdown 컴포넌트. 마이그레이션 0012=recommendations에 base_score·feedback_score(Numeric(6,3) nullable) 컬럼 추가(total_score 의미만 조정 점수로 변경, 하위호환). REQ 접두사 REQ-FW-*(가중치)·REQ-ST-*(투명성). 조정은 파이프라인 시점 일괄 적용(API 재계산 없음). A/B테스트·ML·개인화·어뷰징방지는 범위 밖. spec/tasks/acceptance/progress 4파일. 010 = Phase 11 Docker 컨테이너화 + 운영 환경 설정 — **첫 운영 인프라 SPEC**(기능 아님). 목표: `docker compose up` 단일 명령으로 db+redis+backend+frontend 전체 스택 기동. 신규 6파일: backend/Dockerfile(멀티스테이지·uv·non-root·HEALTHCHECK), backend/.dockerignore, api/routes/health.py(DB SELECT 1·Redis PING 점검 `GET /health`, 무인증), frontend/Dockerfile(node:20-alpine→nginx:alpine), frontend/.dockerignore, frontend/nginx.conf(SPA try_files 폴백·/api 프록시·gzip·캐시). 수정: docker-compose.yml(확장: 기존 postgres+redis에 backend/frontend·healthcheck·redis_data 볼륨 추가), .env.example(확장), api/main.py(인라인 /health → 라우터 치환 + CORS_ORIGINS), 필요 시 routes/__init__.py·.gitignore. REQ 접두사 REQ-DKR-*·REQ-HC-*·REQ-ENV-*·REQ-NGX-*·REQ-NFR-*. AC-1~15. K8s/CI-CD/TLS/시크릿매니저/모니터링스택은 범위 밖. spec/tasks/acceptance/progress 4파일. 011 = Phase 12 GitHub Actions CI/CD 파이프라인 — **두 번째 운영 인프라 SPEC**(SPEC-010 Docker 산출물 위에 구축). CI(push/PR→master): backend `ruff check`+`compileall src`(타입체커 부재로 mypy/pyright 미도입)+`pytest --cov-fail-under=85`, frontend `npm run lint`+`npm test`(vitest)+`npm run build`(tsc), Docker 빌드 검증(backend/frontend Dockerfile, push 안 함). CD(master push만): ghcr.io에 `ai-stock-picker-backend`·`ai-stock-picker-frontend` 빌드·푸시, 태그=`latest`+git SHA(format=long), 인증=기본 `GITHUB_TOKEN`(`packages: write`)만. **비자명 사실: 프론트엔드에 ESLint 설정·`lint` 스크립트가 부재**(package.json에 lint 없음)→M1 선행으로 `frontend/eslint.config.js`(ESLint9 flat)+`lint` 스크립트+devDep 추가가 SPEC 스코프에 포함됨(REQ-CI-FE-LINT). 신규 파일: `.github/workflows/ci.yml`·`cd.yml`(또는 통합), `frontend/eslint.config.js`. REQ 접두사 REQ-CI-BE-*·REQ-CI-FE-*·REQ-CI-DKR-*·REQ-CD-*·REQ-NFR-*. AC-1~9. K8s/클라우드배포/스테이징·프로덕션 실배포/Vault·시크릿매니저/TLS/모니터링/릴리스태깅은 범위 밖. spec/tasks/acceptance/progress 4파일. 013 = Phase 14 알림·모니터링 시스템 — **신규 모듈은 인앱 알림 인박스만**. 5개 요청 중 관심종목등록=Phase E `watchlist/`(마이그0006)·가격알림CRUD=Phase F `notifications/alert_*`(마이그0007)·`check_price_alerts`(5분 IntervalTrigger)·`_trigger_alert`(텔레그램`_send_message_sync`+이메일`send_price_alert_email`, 발동후 is_active=False)는 **이미 완성**→재사용·연동만. 진짜 신규=(1)`notifications` 테이블(마이그 0014, down_rev=0013): id,user_id(FK CASCADE),type('price_alert'|'rec_new'|'rec_dropped'),krx_code,title,body,is_read(default False),ref_date(Date nullable, 추천변경 중복방지용),related_alert_id(FK watchlist_alerts nullable),created_at,read_at + **UNIQUE(user_id,type,krx_code,ref_date)**(장중30분 재실행 멱등성). **Notification 모델은 models.py에 아직 없음**(마지막=RecommendationFeedback)→신규. (2)인박스 서비스/라우터: GET /notifications(unread_only·limit≤100), GET unread-count, PATCH {id}/read, PATCH read-all, 전부 get_current_user 보호·타사용자 404. (3)가격알림→인박스: `_trigger_alert`에 create_notification(type='price_alert',ref_date=None) 추가(텔/메일 실패와 독립). (4)추천변경감지 `notifications/rec_change.py`: 현재 trade_date vs 직전 distinct trade_date 추천 유니버스 차집합→진입=rec_new/이탈=rec_dropped, **관심목록(WatchlistItem) 보유 사용자만** 알림, ref_date=현재 trade_date. recommendations 테이블이 진실소스(스냅샷 테이블 불필요). 첫 실행시 dropped 없음. (5)파이프라인 연동: run_recommendation **직후** detect 호출(run_daily_pipeline·run_intraday_pipeline), 예외격리(run_sector_aggregation 패턴), **신규 타이머 미도입**. 프론트: NavBar 종아이콘+미읽음배지(unread-count 폴링)·Notifications 페이지·기존 api/notifications.ts 확장. **경로주의: 기존 email_router가 이미 prefix `/notifications`**→충돌시 인박스를 `/notifications/inbox`로 분리(RUN에서 검증). REQ 접두사 REQ-NOTI-*·REQ-PA-*·REQ-RC-*·REQ-MON-*·REQ-FE-*·REQ-NFR-*. AC-1~20. 제외: 신규채널(웹푸시/SMS/카톡)·WebSocket실시간푸시·관심종목CRUD재구현·가격알림CRUD재구현·알림설정UI·랭크변동세분화알림·ML우선순위·보존기간정책. spec/tasks/acceptance/progress 4파일. **마이그레이션 최신=0014**(이전 백테스트0013).

021 = Phase 21 뉴스피드·AI 시장 템포 (News Feed & AI Market Sentiment) — **재사용 SPEC, 신규 테이블·마이그레이션 없음**. **⚠️ 핵심: 뉴스 수집+Claude 감성분석+종목매핑+뉴스피드 인프라가 이미 전부 존재**→요청서의 `news_articles` 테이블(마이그 0018)·`url_hash`·`krx_codes` 콤마컬럼·인라인 Claude 호출·feedparser 전부 거부. 기존 자산: `articles` 테이블(Article: id Int PK, url String(2048) UNIQUE=중복방지, source, title, content?, published_at?, collected_at, status collected→analyzed→done), `analysis_results`(AnalysisResult: article_id FK, sentiment positive/negative/neutral, sentiment_score Numeric(4,3) -1~1, sentiment_label 5단계 한국어, sector_tags[], keywords[], summary, analyzed_at), `stock_mentions`(StockMention: article_id FK, stock_name, krx_code String(10) nullable, mention_status), `analysis/client.py ClaudeAnalysisClient`(anthropic.AsyncAnthropic, claude-haiku-4-5, 3회 재시도, 실패 None), `collectors/`(CollectorService.collect_all: hankyung·mk RSS + naver=연합뉴스 경제 RSS, **httpx+stdlib xml.etree, feedparser 미사용 의도적**, URL UNIQUE 멱등, 소스별 실패격리), `sentiment_label.py score_to_label`(매우긍정≥0.6/긍정≥0.2/중립>-0.2/부정>-0.6/매우부정), `api/routes/news.py GET /news`(limit만, krx_code 필터 없음, NewsItem/NewsResponse), 스케줄러 `run_intraday_pipeline`(09-15시 30분)+`run_analysis`가 이미 30분 수집·분석. **진짜 신규**=(1)`GET /news/market-sentiment`(최근24h analysis_results 평균 sentiment_score+positive/negative/neutral 분포+score_to_label 톤+as_of, 0건 시 avg_score=null·"데이터없음", Redis `market_sentiment:24h` TTL1800)·(2)`GET /news?krx_code`(stock_mentions 조인 종목필터, krx_code NULL 기사 제외, 미제공 시 기존 동작 유지·하위호환, Redis `news:{krx_code}` TTL1800)·(3)`POST /news/fetch`(무인증 수동트리거=CollectorService+run_analysis 1회, partial graceful, {collected,analyzed})·(4)프론트 `MarketSentimentWidget`(게이지 green/red/gray+뉴스리스트, **프론트 news 코드 전무·Dashboard.tsx 없음**)+`api/news.ts`+종목별 뉴스(StockDetailPage 연동). 신규 마이그레이션 없음(0017 유지, SPEC-019 패턴). REQ 접두사=**하위 스코프**(기존 수집코드 REQ-NEWS-004~006 충돌회피): REQ-NEWS-FEED-*·REQ-NEWS-SENT-*·REQ-NEWS-FETCH-*·REQ-NEWS-CACHE-*·REQ-NEWS-API-*·REQ-NEWS-FE-*·REQ-NEWS-NFR-*. M1~M6. 제외: 자동매매·news_articles테이블·신규마이그·url_hash·krx_codes비정규화·신규Claude호출·feedparser·신규수집기/소스·신규스케줄러잡·GET /news재구축·개인화뉴스·실시간푸시/알림연동·감성시계열/섹터감성/뉴스검색/페이지네이션. **spec/progress 2파일**(019·020 패턴). **마이그레이션 최신=0017 유지(021 신규 없음).**

020 = Phase 20 알림·알림 설정 (Price Alert & Notification System) — 사용자 정의 알림 규칙 3종(target_price 목표가·surge_drop 급등락±%·ex_dividend 배당락 N일전). **⚠️ 핵심: 알림 인프라가 이미 대거 존재→재사용 SPEC**. 신규 `notifications` 테이블 생성 금지(이미 마이그 0014, SPEC-013), 인박스 엔드포인트(inbox_router.py: GET /notifications/·unread-count·PATCH {id}/read·read-all) 재구축 금지, `watchlist_alerts`(마이그0007)·`/watchlist/alerts`·`check_price_alerts`·`_trigger_alert`·프론트 NavBar 벨/배지/Notifications.tsx 전부 불변. **진짜 신규**=surge_drop·ex_dividend 유형(둘 다 코드에 없음; ex_dividend는 SPEC-019가 명시적 제외했던 영역)·알림 수정 PUT·통합 `alerts` 테이블(마이그 0017 down_rev=0016, **alerts만 생성**). Alert 컬럼: id(**Integer PK**, 요청서 UUID 거부—코드베이스 전체 Integer autoincrement 규약), user_id FK CASCADE, krx_code, stock_name?, alert_type String(20), condition_value Float, condition_direction String(8)? (above/below/either), is_active, is_triggered, triggered_at?, triggered_message String(500)?, created_at. 신규 라우터 prefix `/alerts`: POST/GET/PUT {id}/DELETE {id}(인증·소유권404/401)+POST /alerts/check(무인증 수동트리거). 점검: target_price=close_price vs value, surge_drop=change_rate% vs value, ex_dividend=오늘==기준일-N일(기준일 미확보 시 미발동·추측금지). 데이터=mapping/prices.py get_stock_price_data + SPEC-019 portfolio/dividends.py 배당조회(Redis 캐시 재사용). 발동→기존 notifications 테이블 적재(type∈{target_price,surge_drop,ex_dividend}, ref_date=triggered_at.date()로 UNIQUE 멱등)+이메일 베스트에포트. **이메일=기존 email_service.py smtplib 재사용(aiosmtplib 미추가), env `SMTP_PASSWORD`(요청서 SMTP_PASS 오기)·SMTP_HOST/PORT/USER/FROM, SMTP_HOST 미설정 시 graceful skip**. 스케줄러 신규 잡 `check_alerts`(IntervalTrigger 기본10분, 예외격리, 기존 check_price_alerts 불변). 프론트 신규=/alerts 페이지만(벨 재사용). REQ 접두사 REQ-ALERT-*·REQ-ALERT-CHECK-*·REQ-ALERT-NOTI-*·REQ-ALERT-EMAIL-*·REQ-ALERT-API-*·REQ-ALERT-SCHED-*·REQ-ALERT-FE-*·REQ-ALERT-NFR-*. M1~M7. 제외: 자동매매·notifications테이블재생성·인박스POST별칭·watchlist_alerts수정·UUID·aiosmtplib·Celery·신규채널·WebSocket푸시·배당전체수집잡·배당락추측·자동재무장. **spec/progress 2파일**(019 패턴). **마이그레이션 최신: 020이 0017 추가(alerts만)**. **비자명: 목표가 알림은 watchlist_alerts(기존)와 alerts(신규) 양쪽 공존→이중발송 가능(의도된 동작으로 문서화).**

023 = Phase 23 주식 알림 강화 (조건별 가격/거래량/추천 알림) — **재사용 SPEC, 신규 테이블·마이그레이션 없음(0017 유지)**. 요청 5종 중 (1)급등락=`surge_drop`·(4)목표가=`target_price`·(5)실시간임계값=target_price(장중주기)는 **이미 SPEC-020 alerts 테이블로 구현완료→재사용·문서화**. **진짜 신규 3종**: (2)거래량급증 `volume_spike`(alerts.alert_type에 값 추가, condition_value=배수 기본2.0, 당일거래량≥30일평균×배수 발동; prices.py에 30일평균거래량 산출함수 신규(_fetch_price가 이미 30일df 가져오나 최신1건만 반환·get_stock_price_history는 volume 미포함), 순수함수 check_volume_spike 평균0가드)·(3)추천점수변화 `rec_score_change`(rec_change.py 확장: 기존 check_rec_changes는 진입/탈락 차집합만, 점수델타 미구현→두 trade_date 공통종목 total_score 차이≥0.2 시 관심목록보유자 인박스알림, notifications type값 추가)·(4)장중게이팅(_is_market_open 09:00~15:30 KST 게이트+env토글 ALERT_MARKET_HOURS_GATE). 채널=기존 텔레그램/이메일/인앱 인박스만(신규채널 금지). general_alert_service._VALID_ALERT_TYPES에 volume_spike 추가, check_and_trigger_all_alerts에 분기 추가. 신규 스케줄러잡 없음(기존 check_alerts 10분·파이프라인 check_rec_changes 호출지점 연동). 프론트 신규페이지 없음(백엔드 범위). REQ 접두사=REQ-023-NNN(도메인별 헤더 그룹: 거래량/점수변화/재사용/게이팅/채널/NFR). AC-1~14. M1~M4. 제외: 자동매매·신규채널(웹푸시/SMS/모바일푸시)·WebSocket틱알림·신규테이블/마이그·기존유형재구현·신규프론트페이지·어뷰징방지·점수변동의 추천산식반영·거래량지표고도화. **spec/research/progress 3파일**(022 패턴). **⚠️ alerts 인프라(SPEC-020)·notifications(SPEC-013)·prices.py·rec_change.py 전부 재사용 — 재구현 금지.**

019 = Phase 19 배당 포트폴리오 분석 — 보유 포트폴리오를 배당 투자 관점에서 분석. 종목별 DPS(주당배당금 원)·배당수익률(%)·배당기준일·연간예상배당수익(quantity×DPS)·투자금가중평균수익률·배당캘린더(월별 지급예상)·전년대비 DPS 성장추세. 신규 엔드포인트 `GET /portfolios/{portfolio_id}/dividends`(인증·소유권404), 신규 백엔드 모듈 `portfolio/dividends.py`(ai_analysis.py 형제). **신규 DB 테이블·마이그레이션 없음**(마이그 0016 유지) — 배당데이터는 보유종목 단위 실시간 조회+Redis 캐시 `dividends:{krx_code}` TTL 86400s, prices.py 패턴(run_in_executor+redis.asyncio+graceful degradation) 재사용. 데이터출처=FinanceDataReader 베스트에포트(pykrx 미추가). **핵심 비자명 사실: (1)`stock_fundamentals.dividend_yield`(SPEC-018, 마이그0016) 컬럼은 존재하나 적재 생산자가 코드에 전무**(`StockFundamental(` 인스턴스화는 models.py 정의 외 0건; SPEC-018 M2 일일수집잡은 progress.md에서 DEFERRED, "별도 SPEC(SPEC-STOCK-019) 또는 운영연동 시 구현" 명시). **(2)DPS·배당기준일·지급월 저장소가 코드에 전무**(stock_fundamentals엔 dividend_yield%만). (3)FDR은 국내 DPS/기준일/지급월 캘린더를 안정 제공 못함→다수 N/A 가능. 모든 배당필드 nullable+`dividend_available` 마커+coverage 표시로 정직 노출. **지급월 미상 종목은 캘린더에서 제외**(추측 단언 금지). REQ 접두사 REQ-DIV-*·REQ-DIV-API-*·REQ-DIV-FE-*·REQ-DIV-NFR-*. M1~M6. 제외: 자동매매·전체KRX수집잡(018 M2 영역)·신규테이블·pykrx·세후계산·DRIP·추천재가중·배당락알림·ai-analysis수정·지급월단정. **spec/progress 2파일**(018 패턴). **마이그레이션 최신=0016 유지**(신규 없음). **⚠️ SPEC-018↔019 명명충돌: 018이 019를 펀더멘털수집잡 구현처로 예약했으나 019는 배당분석으로 배정됨→019는 보유종목 실시간조회로 우회하여 018 M2(전체유니버스수집잡)는 여전히 미구현, 향후 별도번호 SPEC 필요.**

018 = Phase 18 종목 스크리너 — 재무 지표(PER/PBR/ROE/시가총액/배당수익률/52주 고저 대비 위치) 기반 필터링. **핵심 비자명 사실: 코드베이스에 PER/PBR/ROE/배당수익률 펀더멘털 저장소가 전무**(AnalysisResult=뉴스감성 전용, mapping/prices.py FDR 래퍼=종가/변동률/거래량만)→신규 일일 수집 잡 + 신규 스냅샷 테이블 `stock_fundamentals`(마이그 0016, down_rev=0015) 적재가 SPEC 스코프. 스크리너는 스냅샷 질의(실시간 아님, 응답<3s). 다중 조건 **AND만**(OR/복합논리 제외). NULL 지표 자동 제외. 시가총액=현재가×상장주식수(상장주식수 미확보 시 NULL). 두 번째 신규 테이블 `screener_presets`(user_id FK, name, criteria JSON, UNIQUE(user_id,name), 사용자당 5개 상한). 신규 라우터 prefix `/screener`: POST /run(인증 선택), GET/POST/DELETE /presets(get_current_user 보호·소유권). 결과 캐시 Redis `screener:{hash}` TTL 300s. 결과 행에서 관심종목추가=기존 WatchlistItem 흐름 재사용. 프론트 /screener(필터 패널+정렬 테이블+로딩). REQ 접두사 REQ-SCR-*·REQ-SCR-PRESET-*·REQ-SCR-API-*·REQ-SCR-DATA-*·REQ-SCR-FE-*·REQ-SCR-NFR-*. M1~M6. 제외: 자동매매·실시간스크리닝·OR복합필터·기술적지표(MA/RSI)·추천재가중·백테스트연동·신규데이터공급자·프리셋공유·알림연동. **spec/progress 2파일**(014 패턴, acceptance 별도 미작성). **마이그레이션 최신=0015**(이전 0014 notifications, 0015 ai_advice). 016·017은 별도 SPEC 존재.

012 = Phase 13 백테스트 엔진 완성 — **신규 아닌 미완성 완성 SPEC**. 백엔드(backtest/router.py·runner.py·metrics.py·schemas.py, main.py에 include_router 완료)·프론트(pages/Backtest.tsx·api/backtest.ts·App.tsx /backtest 보호라우트)·DB(backtest_runs·backtest_daily_results, 마이그 0005) 모두 이미 존재. **핵심 문제=프론트/백엔드 계약 드리프트**: (1)POST /run 응답 백엔드=full BacktestRunResponse vs 프론트 기대={run_id,message}, (2)universe_size·top_n을 프론트는 보내고 BacktestRun model은 기대하나 schema·테이블·러너엔 없음(러너 하드코딩 _TOP_N=5·10종목), (3)status enum 백엔드 `error` vs 프론트 `failed`, (4)results 백엔드=PaginatedDailyResults(거래단위 trade_date/krx_code/signal/price/return_pct) vs 프론트=flat array(date/portfolio_value/benchmark_value/daily_return), (5)벤치마크 백엔드 0건 vs 프론트 benchmark_value 렌더, (6)total_return·win_rate 백엔드 미산출(cagr·max_drawdown·sharpe만). 완성=이 6개 정합화 + 벤치마크(KOSPI `KS11`·KOSDAQ `KQ11`) 수집·정규화 + total_return·win_rate 신규. 마이그 0013=backtest_runs에 universe_size·top_n 추가. REQ 접두사 REQ-BT-RUN-*·REQ-BT-PARAM-*·REQ-BT-METRIC-*·REQ-BT-BENCH-*·REQ-BT-API-*·REQ-BT-FE-*·REQ-BT-NFR-*. AC-1~20. 실거래/주문/실시간·거래비용·신규전략·Redis캐시·리밸런싱정교화·내보내기는 범위 밖. spec/tasks/acceptance/progress 4파일.
- spec/plan/acceptance 본문은 **한국어**로 작성. EARS 키워드(WHEN/WHILE/WHERE/IF...THEN/SHALL)와 코드·식별자만 영어.
- spec.md frontmatter 8필드: id, version, status, created, updated, author(=ircp), priority, issue_number.
- REQ 네이밍은 기능 도메인별 접두사 사용: REQ-NEWS-*, REQ-AI-*, REQ-AUTH-*, REQ-TG-*, REQ-PORT-*, REQ-BT-*, REQ-WS-*, REQ-WL-*, REQ-PAI-*, REQ-FE-*, REQ-NFR-*.
- 002까지는 spec/plan/acceptance 3파일 구조였으나, 003·004·005는 spec/tasks/progress 3파일로 작성됨. 006·007은 spec/tasks/acceptance/progress 4파일(acceptance.md = Given-When-Then). DB 마이그레이션은 누적: 0001~0006 존재(003 watchlist_items=0006), 004 watchlist_alerts=0007·email_subscriptions=0008. 005는 신규 마이그레이션 없음. 006은 0009(컬럼 추가: recommendations.explanation Text nullable, analysis_results.sentiment_label String(20) nullable — 신규 테이블 아님). 007은 0010(신규 테이블 recommendation_feedback: id, krx_code, vote 'up'/'down', user_id nullable FK→users, created_at).
- 기술 스택은 확정·재사용: FastAPI + PostgreSQL + Redis + React + Recharts + FinanceDataReader + APScheduler + Claude API. 신규 SPEC은 기존 테이블·API를 재정의하지 말고 추가만 할 것.

**Why:** 사용자가 단일 일관된 SPEC 스타일과 한국어 본문을 명시적으로 요구함. Phase별로 SPEC을 나누되 번호는 연속.

**How to apply:** 이 프로젝트의 새 SPEC 작성 시 위 ID 체계·한국어 본문·REQ 접두사·frontmatter 형식을 따르고, SPEC-STOCK-001의 자산은 재사용 대상으로 명시.

**영구 제외 (절대 SPEC에 포함 금지):** 자동 매매/주문 실행 — 규제·책임 리스크로 영구 제외. (이메일 알림·소셜 로그인은 Phase 4 연기이지 영구 제외 아님.)

**비자명 코드베이스 사실 (SPEC 작성 시 주의):**
- `GET /recommendations`(api/routes/recommendations.py)는 이미 Redis 캐시(`RecommendationCache`, recommendation/cache.py, TTL 1800s, 키 `recommendations:{date}`)에서만 읽고, 미스 시 scoring을 인라인 실행하지 않고 `PreparingResponse`(preparing)를 반환. 캐시 적재 주체는 스케줄러. → 캐싱 관련 SPEC은 이 동작을 확장으로 정의(인라인 scoring-on-miss를 새로 만든다고 쓰면 코드와 모순).
- `RecommendationItem` 스키마·`recommendations` 테이블에 **섹터 컬럼 없음**. 섹터는 `AnalysisResult.sector_tags`(기사별)·`sector_trends` 테이블에만 존재. 섹터 필터는 추천 적재 시 파생하거나 StockMention→Article→AnalysisResult 조인 필요(SPEC-005 §5.5 방안 A 권장).
- `realtime/price_feed.py` `get_current_price(krx_code)`는 **동기** 함수(FinanceDataReader 직접 호출) + 모듈 내 in-memory `_price_cache` dict 사용(Redis 아님). 호출처: ws_router.py, scheduler/jobs.py(알림 점검), portfolio/service.py(자체 `_get_current_price`). Redis 캐시 추가 시 동기 호환 필요.
- `config.py`에 `redis_url` 기본값 존재, `api/deps.py`에 `get_redis_client`(redis.asyncio, REDIS_URL env)·`get_cache` 의존성 존재.
- 프론트 NavBar(App.tsx)에 이미 `/settings`·`/watchlist`·`/portfolio`·`/backtest` 라우트 존재. Dashboard가 추천을 stock/etf로 분리해 `RecommendationList`/`EtfRecommendationList`에 전달.
- `Recommendation` 테이블에 `reasoning`(Text, nullable, 규칙 기반 `scoring/reasoning.py` `generate_reasoning()` 산출) 컬럼이 이미 존재. SPEC-006은 이를 **제거·치환하지 않고** Claude 자연어 근거를 **별도 `explanation` 컬럼**으로 추가. 두 필드 공존.
- `AnalysisResult.sentiment`는 `Literal["positive","negative","neutral"]`(`analysis/schema.py`), `sentiment_score`는 -1.0~1.0(Numeric(4,3)). SPEC-006의 `sentiment_label`(한국어 5단계)은 점수 기반 파생이며 기존 `sentiment` 보존.
- `ClaudeAnalysisClient`(analysis/client.py)는 `anthropic.AsyncAnthropic` + 모델 `claude-haiku-4-5` 사용. 추천 AI 분석(portfolio/ai_analysis.py)도 haiku-4-5. 새 Claude 호출은 이 패턴 재사용.
- **포트폴리오 AI 분석 실제 엔드포인트 = `POST /portfolios/{portfolio_id}/ai-analysis`**(portfolio/router.py→`analyze_portfolio()` in portfolio/ai_analysis.py). 동기 `anthropic.Anthropic`(Async 아님) 사용, JSON `{diversification, risk, suggestions}` 반환, 면책 `_DISCLAIMER` 포함, **결과 미저장(one-shot)**, Claude 실패 시 `{"error":...}` 딕셔너리 반환(예외 비전파). 보유 종목 없으면 `{"message":...}`로 Claude 미호출. (이전 메모/태스크 설명의 `POST /portfolio/analyze`는 부정확 — 실제 코드 따를 것.) `_get_sector()`는 KRX 코드 앞 2자리 간이 섹터 매핑.
- 추천 히스토리는 `recommendations` 테이블이 진실 소스(캐시는 당일만 보관). `GET /recommendations/history`는 DB 직접 조회로 설계(SPEC-006).
- **섹터 인프라 비대칭(SPEC-008 동기)**: `SectorTrend` 모델·`GET /sectors/trends`(api/routes/sectors.py, 캐시키 `sector_trends:{days}`)·프론트 `SectorTrendChart`(대시보드 렌더)는 모두 존재하나, **`sector_trends` 테이블에 행을 쓰는 생산자가 코드베이스에 없음**(`SectorTrend(` 인스턴스화는 models.py 정의 외에 0건). 섹터 출처는 `AnalysisResult.sector_tags`(ARRAY(String))뿐. SPEC-008이 이 생산자를 신규 구현. 섹터 분석 SPEC은 이 사실 위에 설계할 것.
- 스케줄러 잡(scheduler/jobs.py): `run_daily_pipeline`(06:00)·`run_intraday_pipeline`(09-15시 30분)·`check_price_alerts`(5분)·`send_weekly_email_summary`(월 07:00, **이미 존재**). 주간 다이제스트는 이미 구현됨 → 신규 SPEC으로 만들지 말 것. 텔레그램 notifier(`_send_message_sync`)·이메일(notifications/email_service.py `send_weekly_summary_email`/`send_price_alert_email`) 재사용.
- **⚠️ 스케줄러 미기동 결함(SPEC-022 발견, 2026-06-15)**: `scheduler/jobs.py`의 `setup_scheduler()`는 5개 잡(daily/intraday/check_price_alerts/weekly_email/check_alerts)을 등록하지만, **앱 기동 경로 어디에서도 호출되지 않음**. `api/main.py`의 `_lifespan`은 `price_broadcast_loop`(가격 브로드캐스트)만 백그라운드로 시작하고 스케줄러는 시작 안 함. `grep "setup_scheduler\|.start()"` → telegram bot 스레드만 검출. → 컨테이너/실서버 기동 시 모든 배치·주기 잡이 영구 미실행. 추천 캐시 적재 주체가 스케줄러이므로 `GET /recommendations`가 영구 `preparing`일 수 있음. SPEC-022가 lifespan 연동(+`ENABLE_SCHEDULER` 토글, 단일 인스턴스 가정)으로 수정 예정.

**운영 인프라 사실 (SPEC-010 기준, 배포/Docker SPEC 작성 시 주의):**
- Claude API 키 env는 코드상 **`ANTHROPIC_API_KEY`**(`config.py`의 `Settings.anthropic_api_key` 필수 필드). `CLAUDE_API_KEY` 아님 — 배포 SPEC은 ANTHROPIC_API_KEY로 통일할 것.
- `config.py`(pydantic-settings) 필수 필드: `anthropic_api_key`, `database_url`. 미설정 시 ValidationError로 기동 실패. 선택: `redis_url`(기본 `redis://localhost:6379/0`), `log_level`(기본 INFO).
- DB URL은 **`postgresql+asyncpg://`** 드라이버. `db/session.py`는 async 엔진(`pool_pre_ping=True`) + auth 라우터용 **동기 엔진**(`+asyncpg` 문자열 제거로 파생, TestClient 호환). `DATABASE_URL` env로 재정의 가능, 기본 user/pw/db 모두 `stock_picker`.
- `api/main.py`에 `GET /health`가 **이미 인라인 존재**(`{"status":"ok"}`만 반환). 헬스 체크 SPEC은 이를 라우터로 치환·확장(제거 후 include_router)할 것. CORS `allow_origins`는 현재 `http://localhost:5173`(Vite dev) 하드코딩.
- 루트 `docker-compose.yml`은 **postgres:16-alpine + redis:7-alpine 개발 백킹 서비스만** 정의(backend/frontend 없음, `postgres_data` 볼륨만). `.env.example`도 ANTHROPIC_API_KEY/DATABASE_URL/REDIS_URL/LOG_LEVEL 4키만. 배포 SPEC은 이들을 **확장**(재정의 아님).
- 백엔드 진입점 `stock_picker.api.main:app`, 패키지 매니저 `uv`, Python 3.11 타깃(pyproject `target-version = "py311"`). deps에 `uvicorn[standard]`, `redis>=5.0`, `alembic`. 커버리지 `fail_under = 85`.
- 프론트 빌드 `npm run build`(Vite), `vite.config.ts`·`package.json`·`package-lock.json` 존재(`npm ci` 가능).
- **SPEC-010 Docker 산출물은 이미 커밋됨**(commit `b83b054 feat(SPEC-STOCK-010): Phase 11 Docker`, 확인일 2026-06-15). 추적 파일 8종: backend/Dockerfile·entrypoint.sh·api/routes/health.py·.dockerignore, frontend/Dockerfile·nginx.conf, docker-compose.yml, .env.example. 향후 "Docker 미커밋" 전제 SPEC 요청이 오면 먼저 `git ls-files`로 추적 상태 확인할 것(중복 커밋 방지).
- `health.py`의 `GET /health`는 인라인 버전을 이미 라우터로 치환 완료(`{"status","db","redis"}` 반환, DB SELECT 1 + Redis PING graceful). docker-compose backend HEALTHCHECK가 이 엔드포인트 사용.
- 루트 `.env`(170B)가 git에 추적 중 — 시크릿 위생 주의. backend/.dockerignore는 `.env`/`.env.*` 제외(`.env.example`만 허용)하나 루트 .gitignore 점검 필요.
- env 사용 추가 사실: `REALTIME_POLL_INTERVAL`·`REALTIME_PRICE_MOCK`(realtime 모듈)는 코드에서 참조하나 `.env.example`에 미문서화(둘 다 기본값 코드 내 존재, 동작 무해).

025 = Phase 25 사용자별 알림 채널·유형 설정 (Notification Preferences) — **알림 시스템 아크 완결 SPEC**(013→020→023→024→025). SPEC-024가 이메일·텔레그램을 모든 알림 경로에 무조건 배선→수신 제어 수단 부재가 동기. **진짜 신규=알림 유형별 이메일·텔레그램 ON/OFF**. 신규 테이블 `notification_preferences`(마이그 0018, down_rev=0017): id PK, user_id FK CASCADE, alert_type String(20), email_enabled Bool default True, telegram_enabled Bool default True, updated_at, **UNIQUE(user_id,alert_type)**. 구조=per-(user,type) 1행+채널은 컬럼(대안 per-(user,type,channel) 거부). **핵심 설계=opt-out 하위호환**: 설정 행 없으면 두 채널 모두 활성으로 간주→SPEC-024 동작 불변. 게이트 헬퍼 `is_channel_enabled_async`(general_alert_service, AsyncSession)+`is_channel_enabled_sync`(rec_change, 동기 Session) **양쪽 필요**(세션 타입 상이). 디스패치 4지점 게이트: general_alert_service `_try_send_alert_email`/`_try_send_telegram`(target_price/surge_drop/volume_spike)+rec_change `check_rec_changes`(rec_new/rec_dropped)/`check_rec_score_changes`(rec_score_change). **인앱 인박스 알림=토글 대상 아님, 항상 생성**. 게이트 조회 실패=fail-open(기본 발송, 알림 누락 회피). 설정 7유형=target_price/surge_drop/volume_spike/ex_dividend/rec_new/rec_dropped/rec_score_change(레거시 price_alert 제외). API `/notifications/preferences` GET(7유형 기본값 채움)+PUT(upsert, 미지원유형 422), 동기 라우터(get_current_user+get_db_session) 패턴. 프론트 Settings.tsx에 유형×채널 매트릭스 추가(기존 이메일 구독 섹션 보존)+api/notifications.ts 확장. REQ 접두사 REQ-PREF-*·REQ-PREF-API-*·REQ-PREF-DISPATCH-*·REQ-PREF-FE-*·REQ-PREF-NFR-*. AC-1~11. M1~M5(T{N}-{M}). 제외: 자동매매·인박스토글·레거시watchlist_alerts변경·신규채널(웹푸시/SMS/슬랙)·채널추상화레이어·종목별세분화·quiet hours·다이제스트빈도·스케줄러수정(022). **spec/research/progress 3파일**(023·024 패턴). **마이그레이션 최신=0018**(notification_preferences).

027 = Phase 3(로드맵) "포트폴리오 AI 최적화" 두 번째 SPEC — **리스크 분석 및 상관관계 매트릭스, 신규 테이블·마이그레이션 없음(0018 유지)**. SPEC-026이 명시적으로 연기한 "리스크 상관관계 매트릭스"를 구현. **진짜 신규**=(1)`GET /portfolios/{portfolio_id}/risk-analysis?period={int}&refresh={bool}`(async, 인증·소유권): correlation_matrix(dict[str,dict[str,float]], 외부/내부 키 모두 krx_code, 대각 1.0, -1~1)+holdings_volatility[{krx_code,name,annualized_volatility_pct,price_data_days}]+portfolio_volatility_pct(float)+diversification_benefit_pct(float, 음수 클램프 0)+period_days+calculated_at. period ∈ {30,60,90,180,252} 기본 90, 외 값 422. 유효 종목 <2개 400. (2)신규 서비스 `portfolio/risk_analysis.py`(ai_analysis.py·service.py 형제): calculate_risk_analysis(portfolio_id,user_id,db,redis,period=90,refresh=False), FDR 종가 시계열 run_in_executor(prices.py 패턴)·실패 종목 제외, **numpy** 계산(상관=np.corrcoef/pandas.corr 대각1·공통일부족쌍 0.0, 변동성=std×√252×100, 포트폴리오변동성=sqrt(wᵀΣw)×√252×100, 분산효익=(1-port/weighted_avg)×100). (3)Redis 키 `portfolio_risk:{id}:{period}:{date}` TTL3600s ?refresh=true 갱신 graceful. (4)프론트 `RiskAnalysisPanel.tsx`(상관 히트맵 적색↔흰색↔청색+변동성 테이블+요약+기간선택기)+Portfolio.tsx(PortfolioScoreCard 아래 렌더)+api/portfolio.ts apiGetRiskAnalysis. **⚠️ numpy>=1.26 직접 의존성 추가(FDR 전이 의존성이나 직접 사용→명시), scipy 금지(REQ-RISK-NFR-001).** **⚠️ finance-datareader>=0.9 이미 pyproject.toml에 존재(line 12).** REQ 접두사 REQ-RISK-*·REQ-RISK-PERIOD-*·REQ-RISK-DATA-*·REQ-RISK-CONSTRAINT-*·REQ-RISK-AUTH-*·REQ-RISK-CACHE-*·REQ-RISK-NFR-*·REQ-RISK-FE-*. AC-1~11. M1~M5. 테스트=단위 8(test_correlation_matrix_calculation 등)+통합 3(test_risk_analysis_endpoint_200/400/422). 의존성=SPEC-026(Portfolio.tsx 구조)·SPEC-017(get_portfolio_with_holdings·비중). 제외: 자동매매·scipy/MPT/효율적프론티어·MonteCarlo·VaR/CVaR·Sharpe/Sortino/Beta·미래예측·세금/거래비용·해외자산(SPEC-028)·상관/변동성 시계열(rolling)·위험한도알림·optimize수정·신규마이그레이션. **spec/plan/acceptance/progress 4파일 + spec-compact.md**(026 패턴).

026 = Phase 3(로드맵) "포트폴리오 AI 최적화" 첫 SPEC — **서술형→처방형 업그레이드, 신규 테이블·마이그레이션 없음(0018 유지)**. 기존 `portfolio/ai_analysis.py`는 동기 `anthropic.Anthropic()`+claude-haiku-4-5+max_tokens512, 반환 3필드(diversification/risk/suggestions 정성설명)뿐+코드에 `@MX:WARN`(동기 블로킹) 플래그됨. **진짜 신규**=(1)`POST /portfolios/{portfolio_id}/optimize?refresh=bool`(async, 인증·소유권404): score(int 0-100)+score_breakdown{diversification,risk_balance,momentum 각0-100, 평균=score}+target_weights[{krx_code,current_pct,target_pct,action 'buy'|'sell'|'hold',delta_shares int}]+new_stocks[{krx_code,name,sector,reason} 최대5·미보유·recommendations 상위]+summary(한국어3-4문장+면책). 모델 유지 claude-haiku-4-5, **max_tokens 1024로 확장**, **AsyncAnthropic 전환**. 입력=보유종목 sector·weight_pct·current_price+최신 추천점수(recommendations 조인, 최신 trade_date). 제약=단일종목≤30%·단일섹터≤40% 프롬프트 포함. action 임계=±2%p(서버측 보정). (2)Redis 캐싱 키 `portfolio_optimize:{portfolio_id}:{date}` TTL3600s, `?refresh=true` 강제갱신(api/deps.py redis.asyncio 재사용). (3)프론트 Portfolio.tsx "AI 최적화 분석" 탭+신규 컴포넌트 PortfolioScoreCard/RebalancingTable/NewStockSuggestions+api/portfolio.ts optimizePortfolio(). (4)**M4 버그수정**: 기존 `POST /portfolios/{id}/ai-analysis`도 동기 Anthropic→AsyncAnthropic 전환(@MX:WARN 해소). **⚠️ 데이터출처 정정: 요청서 "daily_recommendations" 테이블은 코드에 없음→실제 `recommendations` 테이블(db/models.py:157 Recommendation, total_score·trade_date) 사용.** **⚠️ router prefix=`/portfolios`(복수), ai-analysis 실제경로=POST /portfolios/{id}/ai-analysis(router.py:106-107 def ai_analysis).** REQ 접두사 REQ-OPT-*·REQ-OPT-ACTION-*·REQ-OPT-CONSTRAINT-*·REQ-OPT-DATA-*·REQ-OPT-NFR-*·REQ-OPT-FE-*. AC-1~9. M1~M5(T{N}-{M}). 의존성=SPEC-017(성과·섹터)·SPEC-009(추천점수). 제외: 자동매매·scipy/MPT·MonteCarlo·VaR/Sharpe·세금/거래비용·해외자산(SPEC-028예정)·상관관계매트릭스(SPEC-027예정)·포트폴리오버전히스토리·실시간스트리밍·신규마이그레이션. **spec/plan/acceptance/progress 4파일 + spec-compact.md**(run단계용 압축본).

**022 = Phase 22 Docker 컨테이너화 커밋 및 운영 환경 검증** — **운영 인프라 검증 SPEC**. 요청서 전제("Docker 미커밋")는 검증 결과 오류(이미 커밋됨)→스코프를 "현재 코드베이스(Phase 21 반영) 대상 검증 + 결함 수정"으로 재정의. **핵심 가치=스케줄러 lifespan 미연동 결함 수정**(위 항목 참조). REQ 접두사 REQ-022-COMMIT/SCHED/MIG/ENV/VERIFY/NFR-*. AC-1~13. M1~M5. 신규 마이그레이션 없음(0017 유지). 제외: 자동매매·원격push·CI/CD(011영역)·K8s·TLS·신규마이그·신규스케줄러잡·ai-analysis수정·분산스케줄러. **spec/research/progress 3파일.**
